# False Friends image exports

All required website assets are now present, including localized screenshots, the app icon, browser icons, the Apple touch icon, and localized 1200×630 social cards.

The localized Fastlane screenshots are stored under `/assets/faux-amis/en-US/`, `/es-ES/`, `/fr-FR/` and `/it/` and are already wired into their matching pages.

The site uses the supplied app tint `rgb(255, 56, 60)` directly for accents, buttons and principle cards.
